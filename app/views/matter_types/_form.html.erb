<%= form_with(model: matter_type) do |form| %>
  <% if matter_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(matter_type.errors.count, "error") %> prohibited this matter_type from being saved:</h2>

      <ul>
        <% matter_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
